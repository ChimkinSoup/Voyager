/// Code-configurable sync debounce duration (not user-facing).
const int syncDebounceSeconds = 1;

/// Soft-delete retention before permanent purge.
const int softDeleteRetentionDays = 30;

/// Alpha value used for thin accent-colored borders that mark state
/// (selection, focus, "hot" values) without a blurred glow.
const double accentBorderAlpha = 0.9;
