/// Stable built-in calendar id used locally. It may be renamed/recolored, but
/// remains the default and cannot be deleted. Calendars are local-only (no
/// Firestore sync), so no Firestore-safe alias is needed.
const legacyCalendarId = '__legacy_calendar__';
