# FEEDBACK
Here is some feedback, implement fixes in the best way you see fit. If there is any unclarity, ask questions before proceeding. If the change is applicable to other parts of the app (Such as a universal change like changing a textbox or a repeated UI), apply the change to wherever necessary.
- [ ] When i try to drag a task around in the to-do list, i will drop it where I want, and then for a second it flickers back to where it was originally, then moves to it's new location. Get rid of this flicker
	- [ ]  Additionally I currently cannot move non-starred tasks into a group of starred tasks. Make it so that I CAN move them (So basically dragging tasks around is the ultimate truth)
	- [ ]  Make it so that I don't have to long press for as long (Reduce by 50%) to move a task around
- [ ] In the search page, I can search fine but I should be able to click a journal entry to have it pop up (Just like the Journal page UI) so I can see it. From there I should also be able to edit it just like from the journal nav
	- [ ]  Additionally do not implement the "keyword mode" and "tag mode", instead default to keyword unless there is a "#" followed immediately (no space) by text, which in that case default it to a tag and search by the tag first, then by the keywords
- [ ] Change the journal flag that is at the top right of a journal entry's title to have a rounded corner, matching the rest of the title text box
- [ ] If I delete a journal, all the entries within it are moved to a journal named "Journal" but this is just creating a new journal with that name. Instead change the implementation so that even after a fresh restart, there is never a time with no journal. By default, there will be a journal named "Journal". This is the enforced app default. Thus you now do not have to worry about the very first journal entry not belonging to a journal. Then, whenever the user creates another journal, then deletes it, all the entries should be moved to the "Journal" that is stored as default. The user is able to rename "Journal" to anything else, but it STILL retains it's default status, so any deleted journals send their journal entries to the default journal. 
	- [ ]  The default "Journal" should be colored as the accent color but let the user change it
	- [ ]  Implement the same thing for the to-do list except call it "To-do"
- [ ] At the top of a journal entry below the title, there is currently no spacing between the date and the delete icon. Add space to it.
- [ ] Add a hitbox at the very bottom of a journal entry where it says it's quote, so the user is able to edit the quote
- [ ] I like the animation when you mark a task as done for the title to be crossed out, but I want it made 50% faster
- [ ] By default, the tasks in the to-do list should be sorted as follows: starred (sorted in the order they were added), tasks with dates (In chronological order, from earliest to latest where earliest is at the top), then unstarred and non-due date tasks (Sorted in the order they were added). Except allow the user to move around the tasks with dates anywhere they want (Again dragging tasks around should be the source of truth)
- [ ] Typing in the notes section of a task (When you click on a task and see the bar on the right) should automatically save it and make the textbox not in focus anymore. You should still be able to do shift enter to add a new line however. Same change should be in place for editing a task's title, however shift enter should NOT add a new line and it should just be equivalent to enter (So it saves the text instead)
- [ ] Add the "Clear due date" button NEXT to the date button when editing a task, there should be some spacing between them so they aren't touching
- [ ] Implement some sort of way to mark a task's due date as only a day, not a time
- [ ] When you try to create a new journal or a new todo list with an empty name it doesn't let you but nothing pops up to notify the user that the name is empty. Instead add a small red text dialogue that states title cannot be empty
- [ ] When you go to edit a task by clicking on it, there should also be an icon that lets you star it (in the right bar)
- [ ] When prompted with a popup to confirm, like confirming a delete, allow the user to press enter on their keyboard to signal yes
- [ ] When you focus on the body of a journal entry to type, the pre-existing text shifts ever so slightly as the text box is focused. Prevent this from happening. I know that by focusing the edges of the text box get slightly bigger, but keep the text where it was before the edges get bolded (So there would be less spacing in the edges but that's fine)
- [ ] When I try to close the right bar that appears when editing a todo task, it stutters for a second then closes. First add an animation when OPENING the bar (So clicking a task to edit it) where it slides out from the right side of the screen, then when you close it, it should slide back to the right side of the screen. Additionally IF the stutter is caused by trying to save locally, then delay the local save so that when the user tries to exit out of the edit bar, then the bar first closes, then it saves locally
	- [ ] If the user is currently editing task A, then switches to task B, the bar on the right should just instantly switch, it shouldn't animate back to the right and then another bar appear (This is the current behavior)
- [ ] Currently if I go into a daily view (Like clicking the small calendar on the left side), then if i click week, month, or year, I am still left stuck on the daily view, so fix this
	- [ ] Additionally the small calendar on the left has way too much spacing horizontally, the icons should be moved much closer together. Also the days that overflow from other months (To make the calendar a full 6 rows) is too light, make the text darker so that it is clear they are not part of the current month. 
		- [ ] I like the calendar view that is shown in the yearly view option, use that or something based off that
	- [ ] In the daily view of a day, add an option next to the day you are viewing so that you can quickly flip between individual days
		- [ ] Additionally add a more faded circle (Compared to the circle that signifies the current date) around the date that is being viewed in a single day view. If they are the same date then only show the more vibrant circle (Do not show this new more faded circle)
	- [ ] Currently the smaller calendar does not show the current date, so add that too (By generating a colored circle around the current date)
- [ ] Currently there is a blue dot on the 18th of June in the calendar when I'm pretty sure there is not supposed to be an event on that date, can you figure out why that is happening and prevent that from happening (If it's not supposed to be there)
- [ ] In the yearly view of the calendar, add a hitbox so that if you click the title of a certain month then you are moved to view that month in monthly view
	- [ ] Add an animation to where it looks like you are zooming into that calendar (In the yearly view) to it then taking up the whole screen (When you are in monthly view)
- [ ] Currently in weekly view it says "Week of" then the last date in the week, so fix that so that it is instead the week of the first day of that week
	- [ ] Additionally in weekly view, add the days of the week at the very top
- [ ] Instead of the triangle flag at the top right of a journal entry's text box to signify that it is in a certain journal, replace that with a bookmark sign, like bookmark_simple from Phosphor but filled in with color. Still keep the gradient except now make it the accent color at the top and 50% of the accent color at the bottom. The bookmark should still be in the top right corner, but move it a little away from the right edge. The top of the bookmark icon should align with the top of the title input textbox. Retain the same functionality, so by clicking you should still see the journals (But now the dropdown menu should also use the same bookmark icons, not triangles), and you should be able to move an entry from journal to journal. Additionally a long title should cut off before overlapping with the bookmark icon
- [ ] When recoloring a journal or choosing a color for a journal, add a faint circle border to colors that have already been chosen (But still let the user pick them freely)
- [ ] In the subtasks section of a task, move the checkbox to the left side, and add the same animation where there is a line that crosses out the text when you click it (And also animate the checkbox being checked)
	- [ ] Make sure the speed of the line matches with the newly increased speed of the animation for full tasks
- [ ] Add a button to delete a task 
- [ ] When I edit the title of a task and click enter, that change is not immediately reflected in the main to-do list view. Make sure this change is reflected first, then it is saved locally and remotely
- [ ] Remove the flag that is shown in a to-do list to reflect the color of the list, instead just make the color of a star when a task is starred, and the color of a checkbox when a task is checked off, the same color as the list color. Additionally change the "Add due date and time" button to reflect the color of the list, along with the the highlight that appears when you click on notes or add subtask or the title text box. 
	- [ ] Ensure that the checkboxes for the subtasks are filled in with the list color too
- [ ] When editing a journal entry, the user hsould be able to edit the title, then press tab to automatically be moved to the journal entry 
- [ ] When i make a new journal entry and edit both the title and the body, i can only see thee title on the left bar that shows all the journal entries, and there is no body. Fix it so that it shows the first little bit of the title and the body
- [ ] Move the trash icon when editing a journal entry from hugging the left side (Where it is placed to the right of the date button) to the very right 
- [ ] Currently I can't drag a starred task around (it is immediately teleported back to the top where the  other starred tasks are), allow the user to move it around freely (And then later, if any new tasks are added, start from the very top task, then move down until the first non-starred task is encountered, where you then insert the new task above that un-starred task)