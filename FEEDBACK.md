# FEEDBACK
Here is some feedback, implement fixes in the best way you see fit. If there is any unclarity, ask questions before proceeding. If the change is applicable to other parts of the app (Such as a universal change like changing a textbox or a repeated UI), apply the change to wherever necessary. If there are contradictory requests, ask before implementing.
## Global App
- [x] When scrolling the nav bar on the very left, the pages are cut off to make room for the inbox and time + weather but can you move those cutoff areas closer to the weather icon and the inbox icon? Also instead of making the cutoff a sharp cut, can you somehow fade out the borders a little
## Journal
- [x] If the user deletes the only entry in a journal, then can you delete it but then automatically make a new empty journal just like what happens when the user switches to that empty journal from another journal
## Search
- [ ] You know how in the search page it shows the title of a journal and also a small blurb of the first couple of words of each journal. Instead if the user searches for something specific, like lets say "AAA" then if a journal had the text "AAA" inside of it, then the small blurb of the body should change to be the text AROUND the word "AAA". Currently if the user searches for "AAA" and the body of a journal has the text "AAA" inside of it but it is very far down, then the blurb would not show the text. Also when the blurb changes, also highlight the word "AAA". (skipped — already implemented; you were on an older build)
## Finance
- [x] Tag suggestion works but the top and bottom tags in the suggestion dropdown menu have highlights that fall short of the very top and bottom border of the dropdown menu, and there is a gap between the border and the highlight that is kept white. Fix this so that it reaches the ends
- [x] Allow the user to right click on transactions to delete them, convert them from expense to deposit, duplicate it (The duplicated transaction should have the date of today but everything else should be the same)
- [x] The sparkline for net worth should be monotonic so that the peaks and troughs do not go below the min or above the max
- [x] Currently the sparkline for net worth has hitboxes for each time period that do not touch, so if you move your cursor between lets say January and February then no informative popup appears since you aren't hovering over anything. Instead extend the hitboxes so that they touch so that the user will always see a popup if they are hovering over the sparkline
- [x] Change the style of the informative popup that appears when the user hovers over a specific datapoint to be like the analytic page popup (With a date + value)
- [x] Allow the user to hover over the pie graph in the "Spending Breakdown" of the analytics view and in the popup it just should repeat the name of the tag, the dollar amount, and the total percentage. Also in both the bottom text and this popup, do not include the "#" so if a tag was "#Dining" it should just show as "Dining"
- [x] Make the bar graphs in the income Vs expense section wider so that they take up more of the window
	- [x] Make the informative hover popup the same style as the analytics page (So include the date and the number)
- [x] During the animation from Weekly to monthly to yearly views of the Income Vs. Expenses page, sometimes the middle date pops up and then goes back down. This happens when I go from year -> month and year -> week, but it could happen for other transitions too. Diagnose and fix this (cause: yearly showed 5 buckets against the others' 12, and fl_chart can't morph two bar lists of different lengths — all three now show 12)
## Life Tracker
- [x] Align the title text along with the note text for the bucket list. Currently the note text (For a completed bucket list event) starts to the left of the start of the event title text
## Settings
- [x] The text box to add a new quote shrinks vertically a little bit when the user types a character, ensure this doesn't happen (the hint was a shorter type slot than the input; fixed for every field)
- [x] Let the user edit quotes that have been added to the quote set
- [x] Ensure that for all text boxes that give the option of "save" or "cancel", if the user presses enter then it should automatically save their text entry and quit the popup (I'm talking specifically about the LeetCode username popup, but if this behavior occurs anywhere else then it should be the same) (the LeetCode popup was the only one missing it)
## LeetCode Tracker
- [x] Make the capsules for each language (Like Python, Java, C, etc) standardized sizes so all of them are the same sizes. Also instead of just coloring the border signifying that is the language chosen, make the whole capsule change colors
- [x] The GraphQL query also returns the lang (Programming language) so also preselect that as the lang of the last successful submission. If there is no lang available then just default to the first one
- [x] Allow the user to right click on questions on the dashboard. The options should be the same as right clicking specific cards in the review deck view
- [x] In the code block, if I enter VIM's normal mode, then use a or i to exit out into insert mode, I'm unfocused from the text box and I can't type in it, fix this (could not reproduce headlessly — added a guard that re-asserts focus when the mode badge unmounts; please retest)
- [x] When viewing the front of a flashcard, make the text description of a question larger, while keeping the example text the same size
- [x] Can you remove the "Edit problem" text at the very top of the editing UI (Same for "Track a problem") and make the "X" floating so that the user is able to click it even if they have scrolled down?
## VIM
- [ ] If the user is in normal mode on an empty line, make their caret wider. Currently it is the same width as the caret when in enter mode, but instead I want it to be of average width for insert mode, like the width of a single space (So the user can still see that they are in VIM mode) (skipped — already implemented; you were on an older build)
- [x] In visual mode, if the user copies text (Using OS controls like ctrl + c), then it should immediately unhighlight everything and leave their cursor where it is currently 
- [x] If I highlight a chunk of text in Visual mode, then switch tabs to another page (Like on my OS, so switching from the Voyager app to Chrome), then switch back to the Voyager app, then my Visual highlight is replaced with an OS highlight, and instead of just highlighting the text it highlights a chunk (Including non-existent characters to make the highlight area a rectangle), fix this
- [x] If the user is in search mode (Like searching for a specific phrase using "/" then entering in a phrase then pressing enter), then they enter search MODE again (by just pressing "/"), then all the previously searched words should be cleared. Currently if the user presses (In this exact order): /, "abc", enter, "/", then without adding any character to search by you can still see all occurrences of "abc" highlighted. I want them to immediately go away when the user presses "/" a second time
- [x] If I enter Visual mode through shift + v (To highlight the whole line), then I am unable to see my caret move left and right. The caret visual stays in the same place, but if i move left enough to bleed over to the previous line, then the highlight changes too, so the caret is being properly computed, just not properly shown. 