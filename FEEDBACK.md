# Feedback
Here is some feedback, implement fixes in the best way you see fit. If there is any unclarity, ask questions before proceeding.
- The debug button that is supposed to delete all journal entries does not delete them
- The log in buttons when opening the app like "continue with google" and "create account" are too close together (They are touching), there should be spacing between them vertically
- In the to-do list, for every task, there should be slightly more spacing between the start of the text and the very left edge of the grey hitbox that appear once your cursor is hovering over the task. Right now the beginning of the text and the left edge of the grey rectangle is almost touching
- For the drop-down menus for the journal name, to-do list name, etc, they currently are bordered by a thin line and are a lighter shade of grey than the rest of the app background. Remove this barrier and instead make it so that you shouldn't be able to differentiate it from the background. (Although don't delete the current implementation, as I might want to roll this back later)
	- Additionally for the to-do list dropdown menu, the menu ends too close to the button to add a new list, ensure there is proper spacing between them
- When you have some journal entries, but then create a new journal, the entries are deleted and you can only see the new journal. instead of this way, make it so that whenever you create your first journal, you will have two journals. One is the new one you named, and the other will be called "Journal" and it will house all the entries you had up to this point. 
- Make it possible to change the name of the journals once they are created. Additionally within that same menu you should be able to assign each journal a color (By default if the user does not select a color, it will be randomly selected from a preset palette, ensuring no repeats until it runs out of colors to choose from). Once you select a certain journal with a certain color, when you look at an entry from that journal, add a small flag, just a small triangle that has a little gradient from white to the color of the journal, that helps indicate the current journal you are on. This flag should be at the top right corner of the title text box. If the title is so long it extends to the very right side, this flag should overlap (be on top of) any text 
	- Implement similar features to the to-do list (Renaming a list title, adding a color flag at the top next to the title of the list this time, not every task, etc)
- Right now if the title of a journal entry is too long, you wrap the text on the left hand side where it shows all the journal entries. Instead of this, just cut off the title after one line. Additionally, take the first sentence of the body of the journal entry, and also show that below the title. Make the title text slightly bigger than the body text
- When you add multiple tasks back to back in the to-do list, every time you enter a task and click enter, the text box is unfocused so you must click again before entering a new task, make it so that even after you add a task your cursor will still be focused.
	- Also remove the text that says "new task" next to the next task option, and make the  "add" button taller, so that it matches the height of the text input box
- Add an animation whenever you finish a task 
- When you try to add a due date for a task, the text that says the date and the calendar icon are too far away from the center, which causes the edges of the grey hitbox to be in line with the text (on the left) and the calendar icon (on the right). Move the text more to the right and the calendar icon more to the left so that there is the proper padding.
	- Also allow the user to specify a time for the task, not just a date
	- When a date is added for a task, currently it is just displayed right below the task title, and the spacing between the task with the date and the next task below it is enlarged to make sure the date has enough space. Instead make the spacing consistent, regardless of date or not, and instead display the date at the very right of the text box. If the task title is so long it would end up overlapping, just cut off the text to ensure the date remains visible. 
	- Also change the UI so that instead of whenever you click on a task to edit it, a pop-up in the middle of the screen appears, there should be a bar that appears on the right of the screen where you can edit the task, but this also still gives you the option of seeing the rest of your tasks. Since I proposed moving the date to the very right of a task, you should move this date so it is not covered by this bar. Similarly, you should adjust the cutoff of the task title so that it also does not get covered up by the bar, or the date. 
		- It should include the same things like editing the title, adding a due date, adding notes, and should also show when you made the task (at the very bottom of the bar)
		- The bar on the left should also allow you to add subtasks. Subtasks are just tasks, lined to a main primary task. They do NOT show up on the main to-do list. Instead, if there are X subtasks, Y of which have been completed, show Y : X below the task (right where the date is shown now), except make the font slightly darker and much smaller. There should still be consistent spacing between tasks without subtasks and those with. 
- Tasks in the to-do list should be able to be starred. Once starred, they are automatically moved to the top of the to-do list, and any subsequent tasks that are added will be added BELOW all the starred tasks (thus the starred tasks should stay on top). Once unstarred, the task should return to where it was originally (or should have been). 
- In the journal page, every journal entry shows it's date. You should also show the time it was made/edited/time user set it to