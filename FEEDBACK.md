# FEEDBACK
Here is some feedback, implement fixes in the best way you see fit. If there is any unclarity, ask questions before proceeding. If the change is applicable to other parts of the app (Such as a universal change like changing a textbox or a repeated UI), apply the change to wherever necessary. If there are contradictory requests, ask before implementing.
- [ ] When I create a new list in todo it should automatically open
- [ ] In the dropdown selection menu for both journal and todo, the checkmark that indicates that you are on the current journal/list should instead be replaced with a border, that borders the hitbox that appears once you hover over the specific selection (Although this border should show constantly, not just when you hover over the specific journal or list), and this should INCLUDE the statistics, so for journals, the number of journals inside that journal should be included inside this border, and same for the todo lists except both the number of completed and uncompleted tasks should be included. 
- [ ] Make the "Add list" button not use bolded text, and make the accent fill 80% opacity. It should be "Add journal" for the journal, and "Add list" for the todo list
- [ ] The color picker in the "new event" popup in the calendar should take up the ENTIRE WIDTH of the popup, not just bunched up on the left side leaving a bunch of empty space on the right. Also make the icons bigger
- [ ] If I go into a month, say September, then shift into the weekly view, then change weeks until I am no longer in September, then try to change back into monthly view, the animation will still change to September. Instead make it change to the current month (Whichever is the latest month in the current week, so if you have March 29, 30, 31, then April 1, 2, 3, 4, then April would be the latest month in the current week, so you would switch to April)
- [ ] In the journal selection dropdown it is supposed to say how many entries are in each journal, but it currently says 0 for all journals other than the one you are currently focused on. 
- [ ] If i take a journal entry from the search page and change it, the change is not reflected in the journal page (This is editing everything, like changing the title or the body or changing which journal it belongs to or changing the mood), ensure that ALL changes to the journal in the search page are reflected
- [ ] Add more padding between the hitbox for the journal/list selector (For each individual option) and the statistics shown. Also move the statistics up so that they are centered 
	- [ ] Reduce the padding between the triple dots and the right edge too
- [ ] In the todo list, if the user creates a new list then it should be opened by default. Same behavior should be kept in Journal
- [ ] If the user is viewing all journals, but then they click on the dropdown menu to select an individual journal, that should be opened and the "All journals" toggle should be turned off
- [ ] If you press enter when editing a quote it should save the quote and automatically close the popup
- [ ] Currently the "Add list" and "Add journal" buttons are fully filled in, but instead only have color directly around the text, in a small rectangle with rounded corners. But if the user hovers over the button, then it should highlight the entire width of the dropdown menu
- [ ] If a user changes a task from being in list A to then being in list B, the view should STAY on list A, and not change to list B
- [ ] Add the same draggable feature to subtasks as current main tasks have
- [ ] Make sure that subtask titles are centered to the checkbox. Additionally if a user hovers on the subtask to edit the name, there should be a grey hitbox like on main tasks. Make the text box that appears longer vertically too
- [ ] If i open the dropdown menu to select a todo list and then switch to a different page and switch back, it is still visible for a frame or two until it disappears, ensure this does not happen
- [ ] Currently under a task if there are subtasks, in the main list view it shows "X : Y". Change it so that it is now "X | Y"
- [ ] In settings if you try to add a color to the color palette then the "Add custom color" text goes to the top of the text box, but it does not center itself at the top of the border of the text box (Like all other text boxes in the app), ensure the behavior is changed so that the text "Add custom color" then overlaps with the top border of the text when the text box is in focus
- [ ] Make the border around each color swatch when choosing the color of a journal more obvious (The one that indicates that other journals have used this color too)
- [ ] If i edit the name or change the color of a journal, the old title+color is still shown in the dropdown menu as it closes (After closing the popup to edit the journal aspects), i want it to immediately reflect the changes. The changes ARE reflected next time the dropdown menu is opened, but I want it to show the changes as it closes. Tell me if this is not possible with the current setup 
- [ ] When the user tries to make a new event in the calendar, the title text box should be automatically in focus. Additionally if the user clicks enter while NOT focused in on a text box, it does not add the event. Make sure it does
	- [ ] Implement this feature everywhere, so for task editing if you are not focused in on a text box but click enter it should save
