Currently if i edit the journalling page, then i have to wait until my changes are saved locally before switching to the search page to see the updated changes. Make it so that the changes are saved automatically (Both locally and remotely) when the user switches away from the journal page, not just after the debouncer.

Also, what happens when the user edits a journal, waits until it saves locally, but then switches page, does it also still save remotely? 


Before starting your implementation, tell me how feasible it is to make it so that both the journal edits and search page edits save if the user is in the middle of editing, then closes the app. 