# ICON CHANGES
Implement these icon changes, if you cannot find an icon then skip it and mention later that it was not implemented. If the change is applicable to other parts of the app (Such as changing the settings icon from faders-horizontal to sliders-horizontal), apply the change to wherever necessary (Thus remove all instances of sliders-horizontal and replace them with faders-horizontal).
- notebook for the journal nav
- calendar-dots for the calendar nav
- switch to the bold magnifying glass for the search nav
- bug-beetle for the debug nav
- faders-horizontal instead of sliders-horizontal for managing journals or to-do lists